import { FondoBarraDirective } from './fondo-barra.directive';

describe('FondoBarraDirective', () => {
  it('should create an instance', () => {
    const directive = new FondoBarraDirective();
    expect(directive).toBeTruthy();
  });
});
